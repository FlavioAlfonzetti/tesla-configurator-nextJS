const TopBar = () => {
  return (
    <div id="top-bar" className="bg-gray-100 fixed top-0 right-0 left-0 py-2">
      <p className="text-center py-2 font-semibold">
        0% APR available for qualified buyers
      </p>
    </div>
  );
};

export default TopBar;
